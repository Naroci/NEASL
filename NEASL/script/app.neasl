APP:
    NAME="Sample Script!"
    
    PREVIOUS:
        val = lol
        WRITE_LINE(PREVIOUS)   
        WRITE_LINE(val)  
    :PREVIOUS
    
    START:
       WRITE_LINE(1)
       WRITE_LINE(2)
       WRITE_LINE(3)
       value=test
       IF(value=22):
            WRITE_LINE(jo)
       :IF
       TEST()
    :START
    
    TEST:
        WRITE_LINE(lel)
        WRITE_LINE(lel2)
        WRITE_LINE(lel3)
        PREVIOUS()
    :TEST
:APP

OPTIONS:
:OPTIONS